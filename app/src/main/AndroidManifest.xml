<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.psspl.datalogic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DecodeConfigSampleAPI" android:screenOrientation="portrait"/>
        <activity android:name=".DecodeListenerDemo" android:screenOrientation="portrait"/>
        <activity android:name=".DecodeSampleAPI" />

        <uses-library
            android:name="com.datalogic.device"
            android:required="true" />
        <uses-library
        android:name="com.datalogic.extension.selfshopping"
        android:required="true" />
    </application>

</manifest>